# Default: Nix backend. For Guix, set RAILWAY_DOCKERFILE_PATH=Dockerfile.guix
# via `railway variable set` (den spawn --guix does this automatically).
[build]
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ALWAYS"
healthcheckPath = ""
